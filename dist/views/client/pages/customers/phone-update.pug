extends ../../layouts/layouts.pug
block css
block script
    script(src="https://cdn.jsdelivr.net/npm/sweetalert2@11")
    script(src="https://cdn.jsdelivr.net/npm/axios@1.7.9/dist/axios.min.js") 
    script(type="module" src="/client/js/customer/phone-update.js") 

block main
    - if(INFOR_CUSTOMER)
        div(class=``)
            div(class=`container`)

                button(btn-otp = `${ROUTERS.CLIENT.CUSTOMER.PATH}${ROUTERS.CLIENT.CUSTOMER.INFOR_CUSTOMER}${ROUTERS.CLIENT.CUSTOMER.INFOR_CUSTOMER_UPDATE_INFOR}${ROUTERS.CLIENT.CUSTOMER.INFOR_CUSTOMER_UPDATE_EMAIL}${ROUTERS.CLIENT.CUSTOMER.INFOR_CUSTOMER_CREATE_OTP}`) Nhận mã
                input(placeholder='Mã OTP' code-otp)
                input(placeholder='Số điện thoại mới' new-phone)
                button(btn-update = `${ROUTERS.CLIENT.CUSTOMER.PATH}${ROUTERS.CLIENT.CUSTOMER.INFOR_CUSTOMER}${ROUTERS.CLIENT.CUSTOMER.INFOR_CUSTOMER_UPDATE_INFOR}${ROUTERS.CLIENT.CUSTOMER.INFOR_CUSTOMER_UPDATE_PHONE}`) Cập nhật Số điện thoại
